package service

{{- if eq .ProjectType "grpc" }}
type Container struct {
	// gmk appends service fields here
}

func NewContainer() *Container {
	return &Container{
		// gmk appends service constructors here
	}
}
{{- else }}
import (
	"gorm.io/gorm"
)

type Container struct {
	// Add your service interfaces here
	// Example: UserService UserServiceInterface
	// Ensure that these services are registered automatically
	// Do not manually register services here unless absolutely necessary
	// This is to maintain the auto-registration functionality
}

func NewContainer(db *gorm.DB) *Container {
	return &Container{
		// register auto
	}
}
{{- end }}
